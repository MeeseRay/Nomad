extends Node

# Global variables, can be used in any scene and keep track of the value 
var coins = 0
var health = 6
var collectedcoin := []
var coinname: String
# Called when the node enters the scene tree for the first time.
func _ready():
	pass
	#print(collectedcoin)
	#if coinname in collectedcoin:
		#queue_free()

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(_delta):
	pass
